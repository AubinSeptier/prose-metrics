# prose-metrics

A static, deterministic analyzer for literary fiction.

`prose-metrics` is a minimalist Python package that extracts statistics on style, rhythm, vocabulary, and dialogue from raw text. Based on `spaCy` and `textstat`, it allows for the technical, deterministic evaluation of prose without relying on AI.
